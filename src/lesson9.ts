function lesson9() {
  // ********* Lesson 9 *********
  // Mapped Types
  // Mappping types modifiers
  // Template Literal Type
  // Fix autocoplete problem for literal union types
  // Satisfies constraint
  // Utility Property key type
  // Utility types overview - Partial, Required, Readonly, Record, Pick, Omit, Exclude, Extract, NonNullable, ReturnType, InstanceType
}
lesson9();
